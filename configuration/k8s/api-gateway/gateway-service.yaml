apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  namespace: backend
  labels:
    group: api-gateway

spec:
  type: ClusterIP
  selector:
    group: api-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080