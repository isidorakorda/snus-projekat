apiVersion: v1
kind: Service

metadata:
  name: sensor-registry
  namespace: backend
  labels:
    group: sensor-registry

spec:
  type: ClusterIP
  selector:
    group: sensor-registry
  ports:
    - protocol: TCP
      port: 81
      targetPort: 8081